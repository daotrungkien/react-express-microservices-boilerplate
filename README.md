A microservices-model based website boilerplate built with ReactJS in the front-end and ExpressJS in the back-end.

To launch the front-end:
```
cd front
npm start
```

To launch the back-end:
```
cd server
npm start
```